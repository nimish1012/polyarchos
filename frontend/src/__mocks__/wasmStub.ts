/**
 * Test stub for the wasm-pack–generated module.
 * Vitest aliases the WASM pkg path here so tests never need a built binary.
 */
export default async function init(): Promise<void> {
  // No-op: WASM init is skipped in tests.
}
